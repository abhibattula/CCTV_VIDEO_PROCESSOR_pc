# CCTV Video Processor PC — pinned dependencies
# Install: pip install -r requirements.txt

# Web framework
fastapi==0.111.0
uvicorn[standard]==0.29.0
aiofiles==23.2.1
python-multipart==0.0.9
Jinja2==3.1.6

# Video processing
opencv-python-headless==4.9.0.80
numpy==1.26.4

# FFmpeg bundled binary
imageio-ffmpeg==0.5.1

# System info
psutil==5.9.8

# HTTP client (used by PyQt6 shell health-check)
requests==2.31.0

# Desktop GUI
PyQt6==6.7.0
PyQt6-WebEngine==6.7.0

# Testing
pytest==8.2.0
pytest-asyncio==0.23.6
httpx==0.27.0

# Optional — object detection mode (install separately)
# ultralytics>=8.2.0

# Optional — visual frame descriptions in intelligence reports (install separately)
# pip install moondream   (~2GB model downloaded on first use to ~/.cache/huggingface/)
